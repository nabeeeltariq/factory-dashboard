{
  "name": "factory-dashboard",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Skipping build, serving static files'"
  },
  "dependencies": {
    "@google/generative-ai": "^0.2.0"
  }
}
